{
  "name": "aitri-hub",
  "version": "0.1.6",
  "description": "Centralized monitoring dashboard for Aitri-managed projects",
  "type": "module",
  "bin": {
    "aitri-hub": "bin/aitri-hub.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "test": "node --test tests/unit/*.test.js tests/integration/*.test.js && npx playwright test tests/e2e/admin-api.test.js",
    "test:web": "npm --prefix web test",
    "test:feature": "npm test && npm run test:web",
    "test:e2e": "npx playwright test tests/e2e/",
    "test:all": "npm test && npm run test:web && npm run test:e2e",
    "test:with-vitest": "node --test tests/unit/*.test.js tests/integration/*.test.js && npx playwright test tests/e2e/admin-api.test.js && npm --prefix web run test"
  },
  "files": [
    "bin",
    "lib",
    "docker",
    ".env.example",
    "README.md"
  ],
  "devDependencies": {
    "@playwright/test": "^1.43.0"
  },
  "dependencies": {}
}
